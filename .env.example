# Required for production
JWT_SECRET=your-secret-key-change-this-in-production

# Optional: For AI testing (works with free tier if not provided)
OPENROUTER_API_KEY=your-openrouter-api-key

# Server port
PORT=3000